apiVersion: v1
kind: Service
metadata:
  name: ecommerce-service
  namespace: ecommerce-bluegreen
  labels:
    app: ecommerce
spec:
  type: ClusterIP
  selector:
    app: ecommerce
    version: blue
  ports:
    - name: http
      port: 80
      targetPort: 3000
